{
  "scripts": {
    "dev": "vite",
    "build": "npm run vercel:check && vite build",
    "build:dev": "vite build --mode development",
    "lint": "eslint .",
    "preview": "vite preview",
    "vercel:check": "node -e \"require.resolve('jspdf-autotable');console.log('Autotable OK')\""
  }
} 